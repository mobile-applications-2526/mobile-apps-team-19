# Production Profile - Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://db.xbkaijymsgwlqwxibdqt.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=VodkaCola7
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# File upload settings (use Supabase Storage)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Supabase Storage configuration
supabase.url=${SUPABASE_URL}
supabase.anon.key=${SUPABASE_ANON_KEY}
supabase.service.role.key=${SUPABASE_SERVICE_ROLE_KEY}
supabase.storage.bucket=event-pictures

# CORS Configuration for Production
cors.allowed-origins=https://your-frontend-domain.com,http://localhost:8081
